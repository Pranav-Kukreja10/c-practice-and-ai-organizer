/*
 * This program demonstrates a simple function call loop.
 * It iterates 3 times and calls the counter() function in each iteration.
 * The counter() function is expected to perform some counting or display logic.
 */

for (int i = 0; i < 3; i++)
    {
        counter(); 
    }

int main() {
    for (int i = 0; i < 3; i++)
    {
        counter(); 
    }
    return 0;
}
